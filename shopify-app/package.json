{
  "name": "partytees-builder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "shopify app dev",
    "build": "shopify app build",
    "deploy": "shopify app deploy",
    "generate": "shopify app generate",
    "info": "shopify app info",
    "extension:dev": "cd extensions/party-builder-extension && npm run dev",
    "extension:build": "cd extensions/party-builder-extension && npm run build"
  },
  "dependencies": {
    "@shopify/app-bridge": "^3.7.10",
    "@shopify/app-bridge-react": "^3.7.10",
    "@shopify/polaris": "^12.0.0"
  },
  "devDependencies": {
    "@shopify/cli": "^3.50.0"
  },
  "workspaces": [
    "extensions/*"
  ]
}
